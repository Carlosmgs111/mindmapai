---
import Layout from "../layouts/Layout.astro";
// Data configuration
const features = [
  {
    icon: "M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12",
    title: "Ingesta Unificada",
    description:
      "Documentos, webs, APIs. Una sola interfaz para todas tus fuentes",
  },
  {
    icon: "M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4m0 5c0 2.21-3.582 4-8 4s-8-1.79-8-4",
    title: "Embeddings Gestionados",
    description: "Generación, versionado y optimización automática de vectores",
  },
  {
    icon: "M13 10V3L4 14h7v7l9-11h-7z",
    title: "Retrieval Inteligente",
    description: "Contexto óptimo para tus LLMs, en tiempo real",
  },
];

const benefits = [
  {
    icon: "🚀",
    title: "A Producción en Horas",
    desc: "No semanas. Integra RAG sin reinventar la rueda",
  },
  {
    icon: "🔧",
    title: "Cero Infraestructura",
    desc: "Olvídate de vector DBs, parsers y orquestación",
  },
  {
    icon: "🎯",
    title: "Menos Alucinaciones",
    desc: "Mejor recuperación de contexto = respuestas más precisas",
  },
  {
    icon: "📊",
    title: "Totalmente Observable",
    desc: "Monitorea cada ingesta, embedding y consulta",
  },
];

const useCases = [
  {
    emoji: "💼",
    title: "SaaS con IA",
    desc: "Añade features de RAG sin complejidad técnica",
  },
  {
    emoji: "🚀",
    title: "Startups",
    desc: "Enfócate en producto, no en infraestructura LLM",
  },
  {
    emoji: "🏢",
    title: "Equipos Empresariales",
    desc: "Escalabilidad y seguridad multi-tenant desde día uno",
  },
  {
    emoji: "🧪",
    title: "Investigación",
    desc: "Experimenta con modelos sin gestionar pipelines",
  },
];
---

<Layout title="Klay - Knowledge as a Service layer">
  <!-- Main Container -->
  <div
    class="min-h-screen bg-slate-950 text-slate-100 font-['Roboto'] "
  >
    <!-- Animated Background Grid -->

    <div class="relative z-10">
      <!-- Header -->
      <header class="container mx-auto px-6 py-4 sticky top-0 backdrop-blur-md z-50">
        <nav class="flex items-center justify-between">
          <div class="flex items-center gap-3">
            <div
              class="w-10 h-10 bg-gradient-to-br from-blue-500 to-cyan-400 rounded-lg flex items-center justify-center"
            >
              <i class="bx bxs-layers-plus-alt text-xl"></i>
            </div>
            <span
              class="text-2xl font-bold font-['Roboto'] bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent"
            >
              Klay
            </span>
          </div>
          <a
            href="/knowledge/assets"
            class="cta-btn px-6 py-2.5 bg-blue-500 hover:bg-blue-600 rounded-lg font-medium transition-all items-center justify-center flex gap-2"
            data-action="start"
            ><i class="bx bxs-terminal text-xl"></i>
            Consola
          </a>
        </nav>
      </header>

      <!-- Hero Section -->
      <section class="container mx-auto px-6 py-24 text-center">
        <div
          class="inline-block mb-6 px-4 py-2 bg-blue-500/10 border border-blue-500/30 rounded-full text-sm text-blue-300 backdrop-blur-sm fade-in"
        >
          <span
            class="inline-block w-2 h-2 bg-blue-400 rounded-full mr-2 animate-pulse"
          ></span>
          Knowledge as a Service
        </div>

        <h1
          class="text-6xl md:text-7xl font-['Roboto'] font-black mb-6 leading-tight slide-up"
        >
          <span
            class="block bg-gradient-to-r from-slate-100 via-blue-100 to-gray-200 bg-clip-text text-transparent"
          >
            El backend de conocimiento
          </span>
          <span class="block text-slate-400 text-4xl md:text-5xl mt-2">
            para tus
          </span>
          <span
            class="block bg-gradient-to-r from-blue-400 via-gray-400 to-violet-400 bg-clip-text text-transparent mt-2"
          >
            LLMs
          </span>
        </h1>

        <p
          class="text-xl text-slate-400 max-w-3xl mx-auto mb-4 leading-relaxed fade-in-delayed"
        >
          Así como <span class="text-blue-400 font-medium">Supabase</span> simplificó
          el backend tradicional,
          <span class="text-gray-400 font-bold">KaaS</span> simplifica la infraestructura
          de conocimiento para IA.
        </p>

        <p
          class="text-lg text-slate-500 max-w-2xl mx-auto mb-12 fade-in-delayed"
        >
          Conecta tus datos. Nosotros hacemos el resto.
        </p>

        <div
          class="max-w-3xl mx-auto mb-12 p-6 bg-slate-900/50 border border-slate-800 rounded-2xl fade-in-delayed-2"
        >
          <div class="space-y-3 text-left text-slate-300">
            <div class="flex items-start gap-3">
              <span class="text-blue-400 mt-1">→</span>
              <span>Ingesta documentos, webs y APIs.</span>
            </div>
            <div class="flex items-start gap-3">
              <span class="text-gray-400 mt-1">→</span>
              <span>Extraemos y limpiamos el texto.</span>
            </div>
            <div class="flex items-start gap-3">
              <span class="text-violet-400 mt-1">→</span>
              <span>Lo particionamos de forma semántica.</span>
            </div>
            <div class="flex items-start gap-3">
              <span class="text-blue-400 mt-1">→</span>
              <span>Generamos y versionamos embeddings.</span>
            </div>
            <div class="flex items-start gap-3">
              <span class="text-gray-400 mt-1">→</span>
              <span
                ><strong>Recuperamos el contexto óptimo</strong> para tus LLMs, en
                tiempo real.</span
              >
            </div>
          </div>
        </div>

        <p class="text-slate-500 mb-8 fade-in-delayed-3">
          Todo desde una sola API, con SDKs ligeros, listo para producción.
        </p>

        <div
          class="flex flex-col sm:flex-row gap-4 justify-center items-center fade-in-delayed-3"
        >
          <button
            class="cta-primary group px-8 py-4 bg-gradient-to-r from-blue-500 to-gray-500 rounded-xl font-semibold text-lg transition-all flex items-center gap-3"
            data-action="try-now"
          >
            Empezar Gratis
            <svg
              class="w-5 h-5 group-hover:translate-x-1 transition-transform"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </button>
          <button
            class="cta-secondary px-8 py-4 bg-slate-800/50 backdrop-blur-sm border border-slate-700 hover:border-slate-600 rounded-xl font-medium text-lg transition-all flex items-center gap-3"
            data-action="demo"
          >
            <svg
              class="w-5 h-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
              ></path>
            </svg>
            Ver Documentación
          </button>
        </div>
      </section>

      <!-- Problem Section -->
      <section
        class="container mx-auto px-6 py-24 bg-gradient-to-b from-transparent to-slate-900/30"
      >
        <div class="max-w-4xl mx-auto text-center mb-16">
          <span
            class="inline-block px-4 py-2 bg-red-500/10 border border-red-500/30 rounded-full text-sm text-red-300 mb-6"
          >
            El Problema
          </span>
          <h2 class="text-4xl font-['Roboto'] font-bold mb-6 text-slate-100">
            Construir RAG hoy es <span class="text-red-400"
              >caro, frágil y lento</span
            >
          </h2>
          <div class="grid md:grid-cols-3 gap-6 mt-12">
            <div class="p-6 bg-slate-900/50 border border-slate-800 rounded-xl">
              <div class="text-3xl mb-3">🏗️</div>
              <h3 class="font-semibold text-slate-200 mb-2">
                Demasiada infraestructura
              </h3>
              <p class="text-sm text-slate-400">
                Vector DBs, parsers, orquestación...
              </p>
            </div>
            <div class="p-6 bg-slate-900/50 border border-slate-800 rounded-xl">
              <div class="text-3xl mb-3">🤔</div>
              <h3 class="font-semibold text-slate-200 mb-2">
                Demasiadas decisiones
              </h3>
              <p class="text-sm text-slate-400">
                Chunking, embeddings, retrieval...
              </p>
            </div>
            <div class="p-6 bg-slate-900/50 border border-slate-800 rounded-xl">
              <div class="text-3xl mb-3">♻️</div>
              <h3 class="font-semibold text-slate-200 mb-2">
                Reinventar la rueda
              </h3>
              <p class="text-sm text-slate-400">
                Cada equipo construye lo mismo
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- Features Section -->
      <section class="container mx-auto px-6 py-24">
        <div class="text-center mb-16">
          <span
            class="inline-block px-4 py-2 bg-green-500/10 border border-green-500/30 rounded-full text-sm text-green-300 mb-6"
          >
            La Solución
          </span>
          <h2 class="text-4xl font-['Roboto'] font-bold mb-4">
            <span class="text-slate-100"> Una capa gestionada </span>
            <br />
            <span class="text-blue-400">entre tus datos y tus modelos</span>
          </h2>
        </div>

        <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
          {
            features.map((feature) => (
              <div class="feature-card group p-8 bg-slate-900/50 backdrop-blur-sm border border-slate-800 hover:border-blue-500/50 rounded-2xl transition-all">
                <div class="w-14 h-14 bg-blue-500/10 rounded-xl flex items-center justify-center mb-6 group-hover:bg-blue-500/20 transition-colors">
                  <svg
                    class="w-7 h-7 text-blue-400"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d={feature.icon}
                    />
                  </svg>
                </div>
                <h3 class="text-xl font-['Roboto'] font-semibold mb-3 text-slate-100">
                  {feature.title}
                </h3>
                <p class="text-slate-400 leading-relaxed">
                  {feature.description}
                </p>
              </div>
            ))
          }
        </div>
      </section>

      <!-- Benefits Section -->
      <section
        class="container mx-auto px-6 py-24 bg-gradient-to-b from-slate-900/30 to-transparent"
      >
        <div class="text-center mb-16">
          <h2 class="text-4xl font-['Roboto'] font-bold text-slate-100 mb-4">
            ¿Por qué KaaS?
          </h2>
          <p class="text-slate-400 text-lg max-w-2xl mx-auto">
            Tú construyes producto. Nosotros gestionamos el conocimiento.
          </p>
        </div>

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6 max-w-7xl mx-auto">
          {
            benefits.map((benefit) => (
              <div class="use-case-card group p-6 bg-slate-900/30 backdrop-blur-sm border border-slate-800 hover:border-blue-500/50 rounded-xl transition-all">
                <div class="text-4xl mb-4 group-hover:scale-110 transition-transform">
                  {benefit.icon}
                </div>
                <h3 class="text-lg font-['Roboto'] font-semibold text-slate-100 mb-2">
                  {benefit.title}
                </h3>
                <p class="text-sm text-slate-400">{benefit.desc}</p>
              </div>
            ))
          }
        </div>
      </section>

      <!-- Use Cases -->
      <section class="container mx-auto px-6 py-24">
        <div class="text-center mb-16">
          <h2 class="text-4xl font-['Roboto'] font-bold text-slate-100 mb-4">
            ¿Para quién es KaaS?
          </h2>
          <p class="text-slate-400 text-lg">
            Equipos que quieren construir IA, no infraestructura
          </p>
        </div>

        <div class="grid md:grid-cols-4 gap-6 max-w-6xl mx-auto">
          {
            useCases.map((useCase) => (
              <div class="use-case-card group p-6 bg-slate-900/30 backdrop-blur-sm border border-slate-800 hover:border-gray-500/50 rounded-xl transition-all">
                <div class="text-4xl mb-4 group-hover:scale-110 transition-transform">
                  {useCase.emoji}
                </div>
                <h3 class="text-lg font-['Roboto'] font-semibold text-slate-100 mb-2">
                  {useCase.title}
                </h3>
                <p class="text-sm text-slate-400">{useCase.desc}</p>
              </div>
            ))
          }
        </div>
      </section>

      <!-- CTA Section -->
      <section class="container mx-auto px-6 py-24">
        <div
          class="max-w-4xl mx-auto bg-gradient-to-br from-blue-600 to-gray-600 rounded-3xl p-12 text-center relative overflow-hidden"
        >
          <div
            class="absolute inset-0 opacity-10 bg-[linear-gradient(rgba(255,255,255,0.1)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.1)_1px,transparent_1px)] bg-[length:24px_24px]"
          >
          </div>

          <div class="relative z-10">
            <h2 class="text-5xl font-['Roboto'] font-black text-white mb-6">
              El conocimiento como infraestructura
            </h2>
            <p class="text-xl text-blue-100 mb-8 max-w-2xl mx-auto">
              Conecta datos, no pipelines. Construye features de IA sin
              reinventar la rueda.
            </p>
            <button
              class="cta-final px-10 py-5 bg-white text-blue-600 rounded-xl font-bold text-lg transition-all inline-flex items-center gap-3"
              data-action="start-free"
            >
              Solicitar Acceso
              <svg
                class="w-5 h-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2.5"
                  d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
            </button>
            <p class="text-sm text-blue-200 mt-6">
              Disponible pronto en beta privada
            </p>
          </div>
        </div>
      </section>

      <!-- Footer -->
      <footer class="container mx-auto px-6 py-12 border-t border-slate-800">
        <div
          class="flex flex-col md:flex-row items-center justify-between gap-6"
        >
          <div class="flex items-center gap-3">
            <div
              class="p-2 square bg-gradient-to-br from-gray-500 to-slate-300 rounded-lg flex items-center justify-center"
            >
              <i class="bx bxs-layers-plus-alt text-xl"></i>
            </div>
            <span
              class="font-['Roboto'] font-bold text-xl bg-gradient-to-r from-gray-400 to-slate-300 bg-clip-text text-transparent"
            >
              Klay
            </span>
          </div>

          <div class="flex gap-6 text-sm text-slate-500">
            <a href="#" class="hover:text-slate-300 transition-colors">Docs</a>
            <a href="#" class="hover:text-slate-300 transition-colors"
              >API Reference</a
            >
            <a href="#" class="hover:text-slate-300 transition-colors"
              >Pricing</a
            >
            <a href="#" class="hover:text-slate-300 transition-colors">Blog</a>
          </div>

          <div class="text-sm text-slate-500">
            Knowledge as a Service • <span class="text-blue-400"
              >Powered by AI</span
            >
          </div>
        </div>
      </footer>
    </div>
  </div>
</Layout>
<style>
  /* Custom Animations */
  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes float-delayed {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-30px);
    }
  }

  @keyframes pulse-slow {
    0%,
    100% {
      opacity: 0.1;
    }
    50% {
      opacity: 0.2;
    }
  }

  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slide-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .orb-1 {
    animation: float 6s ease-in-out infinite;
  }
  .orb-2 {
    animation: float-delayed 8s ease-in-out infinite 1s;
  }
  .orb-3 {
    animation: pulse-slow 4s ease-in-out infinite;
  }

  .fade-in {
    animation: fade-in 0.8s ease-out;
  }
  .fade-in-delayed {
    animation: fade-in 0.8s ease-out 0.2s backwards;
  }
  .fade-in-delayed-2 {
    animation: fade-in 0.8s ease-out 0.4s backwards;
  }
  .fade-in-delayed-3 {
    animation: fade-in 0.8s ease-out 0.6s backwards;
  }
  .slide-up {
    animation: slide-up 1s ease-out;
  }

  .feature-card:hover {
    transform: scale(1.05);
  }
  .use-case-card:hover {
    transform: scale(1.05);
  }

  .cta-btn:hover,
  .cta-primary:hover,
  .cta-secondary:hover,
  .cta-final:hover {
    transform: scale(1.05);
  }

  .cta-primary:hover {
    box-shadow: 0 20px 60px rgba(59, 130, 246, 0.5);
  }
  .cta-final:hover {
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }
</style>

<script>
  // Interactive elements setup
  document.addEventListener("DOMContentLoaded", () => {
    // CTA Buttons tracking
    const ctaButtons = document.querySelectorAll("[data-action]");
    ctaButtons.forEach((button) => {
      button.addEventListener("click", (e) => {
        const action = (e.currentTarget as HTMLElement).dataset.action;
        console.log(`CTA clicked: ${action}`);
        // Add your navigation or tracking logic here
      });
    });

    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
      anchor.addEventListener("click", (e) => {
        e.preventDefault();
        const target = document.querySelector(
          (e.currentTarget as HTMLAnchorElement).getAttribute("href")!
        );
        if (target) {
          target.scrollIntoView({ behavior: "smooth", block: "start" });
        }
      });
    });

    // Intersection Observer for scroll animations
    const observerOptions = {
      threshold: 0.1,
      rootMargin: "0px 0px -50px 0px",
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("fade-in");
        }
      });
    }, observerOptions);

    // Observe feature and use case cards
    document
      .querySelectorAll(".feature-card, .use-case-card")
      .forEach((card) => {
        observer.observe(card);
      });
  });
</script>
